const express = require('express');
const router = express.Router();
const User = require('../models/User');

// Admin specific routes (EJS versions) removed as system has migrated to static HTML + AJAX API.

module.exports = router;
